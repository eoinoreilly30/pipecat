- Fixed `ElevenLabsRealtimeSTTService` WebSocket disconnecting due to idle timeout when no audio is being sent (e.g. when inactive behind a `ServiceSwitcher`). A keepalive task now sends silent audio to maintain the connection.
